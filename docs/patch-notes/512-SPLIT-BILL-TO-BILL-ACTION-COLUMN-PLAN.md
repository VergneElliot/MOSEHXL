# 512 — Split board bill-to-bill DnD + action column (PLAN)

## Context

Partage board was two columns (pool | payments). Staff need to move lines between
payments without returning to the pool, and a dedicated actions column like the POS
cart action rail (but split-specific).

## Scope

1. Drag assigned bill lines onto another payment (and onto the pool to unassign).
2. Three-column layout: pool | Actions | payments.
3. Middle actions: Parts égales, → Paiement N, Répartir… (selection-gated).

## Out of scope

- `@dnd-kit` unification
- Changing catalog « Ajouter » behaviour

## Fiscal impact

MINOR (non-ISCA UX).
