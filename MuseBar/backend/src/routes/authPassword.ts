import express from 'express';

// Password-related auth routes (e.g. reset, change) can be added here.
// For now this router is a placeholder to complete the auth file split.

const router = express.Router();

export default router;

