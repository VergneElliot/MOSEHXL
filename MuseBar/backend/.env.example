# Copy to `.env` and fill in real values (no defaults for secrets).

# Node / environment
NODE_ENV=

# Database
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# Security
JWT_SECRET=

# Security (required in production for legal/archive signing)
ARCHIVE_SECRET_KEY=

# CORS
CORS_ORIGIN=

# Optional (use defaults if omitted)
PORT=
HOST=
DB_MAX_CONNECTIONS=
DB_IDLE_TIMEOUT=
BCRYPT_ROUNDS=
RATE_LIMIT_WINDOW_MS=
RATE_LIMIT_MAX_REQUESTS=

# Optional features (email)
SENDGRID_API_KEY=

# Optional features (legal journal)
LEGAL_JOURNAL_ENABLED=
AUTO_CLOSURE_ENABLED=
SWAGGER_ENABLED=
LOG_LEVEL=
ENABLE_FILE_LOGGING=
ENABLE_DB_LOGGING=

